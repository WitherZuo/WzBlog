/* 修改顶部栏上下边距和背景颜色 */
.header {background: #222}
.header-inner {padding: 60px 0}
/* 改变导航栏菜单项目的基础样式 */
.menu {margin-bottom: 0}
.menu li {margin: 0 4px}
.menu li a {color: #fff !important; font-size: 1em; padding: 0 10px}
.menu li a:hover {background-color: #484848}
.menu-item-active a {color: #fff !important; font-size: 1em; background-color: #484848}
.menu-item-active a, .menu .menu-item a:hover, .menu .menu-item span.exturl:hover {border-bottom: 1px solid transparent !important}
/* 移动端导航栏菜单样式和显示行为优化 */
.menu {+tablet-mobile() {text-align: center}}
.site-nav {background: #222; border: none}
/* 修改站点标题样式 */
.site-subtitle {margin-bottom: 10px}
.site-title {font-weight: 700; font-size: 26px}
/* 文章标题加粗 */
.post-header .post-title {font-weight: 700}
/* 减小文章属性下方边距 */
.post-meta {margin: 3px 0 20px !important}
/* “阅读全文”按钮：字号16px */
.btn {font-size: 1em}
/* 滚动条样式 */
::-webkit-scrollbar {width: 15px; height: 4px; background: #BFBFBF}
::-webkit-scrollbar-thumb {border-radius: 0; background: #222}
html {scrollbar-color: #767676 #1e1e1f; scrollbar-width: auto}
/* 优化文章标题和正文字号，尝试增强其可读性 */
.post-body, table, pre code {font-size: 1em}
/* 强化移动端展开菜单导航按钮显示效果 */
.toggle .toggle-line {background: #fff !important}
/* 分类页面优化 */
.category-all-page .category-list-count {display: none}
.category-all-page .category-list {text-align: center}
/* 修改底部说明文字块样式 */
.footer a {color: #f5f5f5}
.footer {color: #fff; background-color: #222; font-size: 15px}
/* 侧边栏阴影效果取消和背景颜色修改 */
.sidebar {background: #111}
/* 侧边栏切换按钮样式修改修改 */
.sidebar-toggle {transform: scale(1.4)}
/* 侧边栏返回顶部样式调整 */
.back-to-top.motion-element {font-size: 18px}
/* 修正侧边栏切换图标1px偏移 */
.sidebar-toggle .toggle-line-first {margin-top: 1px}
/* 分页指示器调整 */
.pagination .page-number.current {background: #222; border-top: 1px solid #222; border-bottom: 1px solid #222}
.pagination {height: 33px !important; margin-bottom: 0; margin-top: 50px; font-size: 1em}
/* 页面主体内容容器 */
.main-inner {padding-top: 40px; padding-bottom: 56px}
/* 部分页面上下内边距取消 */
.content {padding-top: 0}
.main-inner .posts-expand, .main-inner .posts-collapse {padding-top: 0; padding-bottom: 0}
/* 取消归档、分类、标签等页面最后条目的底部外边距 */
.posts-collapse article:last-of-type .post-header, .category-all-page .category-list-item:last-of-type {margin-bottom: 0}